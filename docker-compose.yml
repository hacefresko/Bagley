version: '3.9'
services:
  mariadb:
    image: mariadb
    environment:
      - MARIADB_ROOT_PASSWORD=xwpogivnoqipwmelvwp4ebqwrv
      - MARIADB_DATABASE=bagley
      - MARIADB_USER=bagley
      - MARIADB_PASSWORD=vgytufqiyoguwpifbnweie
    volumes:
      - ./sql/bagley.sql:/docker-entrypoint-initdb.d/bagley.sql
    ports:
      - 3306:3306

  bagley:
    build: .
    environment:
      - DB_HOST=127.0.0.1
      - DB_NAME=bagley
      - DB_USER=bagley
      - DB_PASSWORD=vgytufqiyoguwpifbnweie
      - DISCORD_TOKEN=write_your_discord_token_here
    cap_add:
      - NET_ADMIN
    depends_on:
      - mariadb
    network_mode: "host"